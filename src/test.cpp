#include "lzipWapper.h"

int main()
{
//    uint8_t inputStream[64 * 4];
//    uint8_t outputStream[64 * 4];
//    uint8_t compputStream[64 * 5] = {0};
//    uint32_t outputStreamLength;
//    uint32_t outputStreamLength3;
//    lzipCompress(inputStream, 64 * 4, outputStream, &outputStreamLength);
//    lzipDecompress(outputStream, outputStreamLength, compputStream, &outputStreamLength3);
    return 0;
}